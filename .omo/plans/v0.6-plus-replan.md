# v0.6+ 重规划 — 2026-06-07 Momus 审核后修正

> **重规划时机**: v0.5a + v0.5b ship 后 (raw_resumes retry 工具闭环), v0.6+ 候选需要 PR 拆分 + 决策点定。
> **依据**: `.omo/plans/v0.6-plus-draft.md` (draft) + `.omo/plans/v0.6-plus-momus-review.md` (Momus 30 项 gap) + `.omo/plans/v0.5-replan.md` (8 段风格参照)
> **关键修正**: v0.5-replan §6 写"v1.0: Phase C 4 项"实际只剩 C.3 + C.5 真未做 (C.2/C.4/C.6 已 ship)

## 1. 计划 vs 实际状态审计 (v0.5 ship 后)

| v0.5-replan §6 候选 | 实际状态 | 决策 |
|---|---|---|
| v0.6 mcp-resume Bheavy 完整版 | ⚠️ draft 已写, 30 项 gap, 需拆 3 PR + 决 RQ/Celery + force 语义 | 修正计划 (本文件) |
| v0.7 skill_mgr 4 工具扩展 | ⚠️ draft 已写, 7 项 gap, 实际 docstring 与 v0.5-replan 描述矛盾 (4 工具 vs 5 函数 vs 1 真注册) | 修正计划 (本文件) |
| v0.8 14 server 并行 spawn 压测 | ⚠️ draft 已写, 5 项 gap, dev 价值低 (v0.5-replan §5 原话) | 修正计划 (本文件) |
| **v1.0 C.2 LLM retry** | ✅ **已 ship** (commit 782a3a9, `apps/api/app/llm/retry.py` 1s→16s 5 retries) | **删除** |
| **v1.0 C.3 .env 整合** | ⚠️ 根目录**无** `.env.example`, 不可"删 root", 需重新定义"审视 + CI check" | 修正: v1.0a |
| **v1.0 C.4 Python 3.14** | ✅ 已 ship (Dockerfile 3.14) | **不在范围** |
| **v1.0 C.5 datetime utcnow** | ⚠️ 4 文件含 utcnow (rate_limit.py / support.py / privacy.py / csm.py), 未做 | 修正: v1.0b |
| **v1.0 C.6 docker healthcheck** | ✅ **已 ship** (dev.yml 行 116-121 web healthcheck + 行 13/27/41/59/91 4 基础设施) | **删除** |

**结论**: v1.0 = v1.0a (.env 整合) + v1.0b (datetime 修复) = 2 PR, 不是 4 PR。

## 2. v0.6+ 候选 4 选 (按 Momus 修正)

| # | 候选 | 估时 | 风险 | 价值 | 决策 |
|---|---|---|---|---|---|
| 1 | **v0.6 mcp-resume Bheavy** (拆 3 PR) | 3.5d | 中 | 高 | ✅ 选 |
| 2 | v0.7 skill_mgr 5 工具 | 2d | 低 | 中 | ✅ 选 (与 v0.6a/b/c **并行 2 agent**) |
| 3 | v0.8 14 server 并行 spawn 压测 | 1d | 低 | 低 | ✅ 选 (主目标=安全验证) |
| 4 | v1.0a .env 整合 | 0.5d | 低 | 中 | ✅ 选 |
| 5 | v1.0b datetime 修复 | 0.5d | 低 | 中 | ✅ 选 |
| 6 | v0.9 candidate_search 归位 | — | — | — | ❌ 条件触发 (看负载) |

## 3. 决策点 (Momus 第 2 个 gap, 必须先决)

按 Momus 6.2 列出 10 项, PR 拆分前**必须**先 sign-off:

| # | 决策点 | 推荐方案 | 理由 |
|---|---|---|---|
| 1 | v0.6a 用 RQ 还是 Celery | **RQ** | 项目轻需求, Redis 已有, RQ 部署 1 worker 进程 |
| 2 | v0.6b WebSocket 鉴权 | **`?token=` 兜底** | CLAUDE.md 模式 5 SSE 鉴权标准 |
| 3 | v0.6c force=True 语义 | **方案 A**: 清空 `rr.candidate_id` + 重建 (旧候选人保留) | 与 v0.5b retry 行为一致, 避免 destructive 副作用 |
| 4 | v0.7 工具命名 | **`list_skills` 复数** (与现有 handler 一致) | skill_tool.py docstring 已用, v0.5-replan §7.4 写 `list_skill` 单数是 typo |
| 5 | v0.7 registry 跨进程 | **进程内 module-level 单例** | mcp-skill-mgr 是单 server 进程, 不需要跨进程 |
| 6 | v0.7 enable/disable 实现 | **选项 α**: list_tools 动态过滤, disabled skill 工具不返回 | 用户视角符合"disable"语义 |
| 7 | v0.8 主目标 | **安全验证** (14 server 并行不死) | dev 数字无意义, profiling 推 v0.8.1 |
| 8 | v0.8 场景 | **3 场景**: stagger 0 / 100ms / 1s | 模拟硬上限 / 快速重启 / 滚动重启 |
| 9 | v1.0a CI check | **强制** (非可选) | CLAUDE.md 强制多层防护 |
| 10 | v1.0b datetime 风格 | **`from datetime import UTC`** (Python 3.11+) | 与 Python 3.14 + 类型注解风格一致 |

## 4. 任务拆解 (按决策点 + Momus PR 拆分原则)

### v0.6a — RQ worker + submit/poll 异步骨架 (1.5d, 2 commit)

**目标**: mcp-resume 异步化基础设施, parse_resume_async 工具端到端跑通。

```
apps/api/pyproject.toml:
  - 加 rq 依赖

apps/api/app/workers/parse_worker.py (新):
  - rq worker 入口 (rq worker -u redis-url parse_queue)
  - 启动:  make celery:watch (类 api_watchdog 双 fork)

apps/api/app/services/parse_task.py (新):
  - enqueue_parse_task(raw_resume_id, content, auto_create=True) -> task_id
  - poll_parse_task(task_id) -> {status, result?, error?}
  - 调 _do_extract_and_link (v0.5a 抽出)

apps/api/app/api/raw_resume.py (新):
  - POST /raw_resumes/parse  (submit: enqueue + 返 task_id + raw_resume_id)
  - GET  /raw_resumes/{id}/status  (poll: 读 task state + 关联 raw_resumes 表)

apps/api/app/mcp_servers/builtin/resume_server.py:
  - 工具加 parse_resume_async (enqueue) + poll_parse_resume (读状态)
  - 旧 parse_resume (sync) 保留, 标 deprecated metadata

apps/api/app/tools/resume_parser.py:
  - _handle_parse_resume_async 拆 submit + enqueue
  - _handle_poll_parse 返 status

apps/api/app/tools/metadata.py:
  - register_tool("parse_resume_async", retryable=True, max_retries=2)
  - register_tool("poll_parse_resume", retryable=False)

docker-compose.dev.yml + prod.yml:
  - 加 celery service (起 rq worker, depends_on redis)
  - depends_on api 改 condition: service_healthy

scripts/check_mcp_servers.py:
  - 验证 resume_server.py 工具列表 ≥ 4 (旧 3 + 新 2 = 5)

apps/api/tests/mcp/integration/test_resume_parser_v0_6a_async.py (新):
  - test_submit_returns_task_id_and_raw_resume_id
  - test_poll_status_processing_when_task_running
  - test_poll_status_parsed_when_task_done
  - test_poll_status_failed_when_llm_failed_3x
  - test_redis_down_on_submit_returns_503
  - test_worker_died_mid_task_status_stuck_processing
```

**v0.6a 退出标准**:
- [ ] 6 新测试全 pass
- [ ] v0.5b 5 测试 + v0.4d 4 测试 = 9/9 仍 pass
- [ ] mcp-resume list_tools 返回 5 工具 (parse_resume + parse_resume_async + poll_parse_resume + batch_parse_resumes + get_candidate_profile)
- [ ] RQ worker 起来, 任务真异步执行 (curl submit 后立刻返 task_id, 不等 LLM)
- [ ] e2e 14/14 仍 pass
- [ ] health-check 14/14

**回滚**: revert 2 commit + 删 docker-compose 2 service + alembic 不动 (无 DB 改动)

### v0.6b — WebSocket 进度推送 (1d, 1 commit)

**目标**: WS 端点 `/raw_resumes/{id}/progress` 推送 processing → parsed/failed 状态变化。

```
apps/api/app/api/raw_resume.py:
  - WS /raw_resumes/{id}/progress
  - 鉴权: header 优先 → ?token= 兜底 (CLAUDE.md 模式 5)
  - 推送: {"status": "processing", "progress": 50} → {"status": "parsed", "result": {...}}
  - 断线重连不重发 (前端自己 poll fallback)

apps/api/app/services/parse_task.py:
  - parse task 内 set_progress(50) 在 LLM extract 前
  - parse task 完成后 set_terminal(parsed/failed) 触发 WS 推送

apps/api/app/mcp_servers/builtin/resume_server.py:
  - 工具加 watch_parse_progress (开 WS)

apps/api/app/tools/metadata.py:
  - register_tool("watch_parse_progress", retryable=False, requires_role=user)

apps/api/tests/mcp/integration/test_resume_parser_v0_6b_ws.py (新):
  - test_ws_pushes_processing_then_parsed
  - test_ws_pushes_processing_then_failed
  - test_ws_auth_via_query_token (CLAUDE.md 模式 5)
  - test_ws_auth_via_header (备选)
```

**v0.6b 退出标准**:
- [ ] 4 新测试全 pass
- [ ] v0.6a 6 测试 + v0.5b 5 测试 + v0.4d 4 测试 = 15/15 仍 pass
- [ ] mcp-resume list_tools 返回 6 工具 (+ watch_parse_progress)
- [ ] curl WS 端点: `wscat -c "ws://localhost:8000/api/v1/raw_resumes/123/progress?token=xxx"` 真收消息
- [ ] e2e 14/14 + health-check 14/14

**回滚**: revert 1 commit (WS 端点 + handler), mcp-resume 6 工具 → 5 工具

### v0.6c — force=True 参数 (1d, 1 commit)

**目标**: retry_raw_resume 扩展 force 参数, 决策点 3 = 方案 A (清空 candidate_id + 重建)。

```
apps/api/app/tools/resume_parser.py:
  - _handle_retry_raw_resume 加 force: bool = False 参数
  - force=True 时: 在 reset status 前先清空 rr.candidate_id
  - 旧候选人**不**自动删 (user 可手动 archive)
  - 调 _do_extract_and_link (auto_create=True 默认, 创建新候选人)

apps/api/app/mcp_servers/builtin/resume_server.py:
  - retry_raw_resume 工具定义加 force 参数

apps/api/app/tools/metadata.py:
  - retry_raw_resume 已有 retryable=False, 不变

apps/api/tests/mcp/integration/test_resume_parser_v0_6c_force.py (新):
  - test_retry_force_false_creates_new_candidate_keeps_old (默认行为, 旧候选人留)
  - test_retry_force_true_clears_candidate_id_and_recreates (force=True 主路径)
  - test_retry_force_true_old_candidate_not_deleted (验证方案 A 不 destructive)
  - test_retry_force_true_creates_different_candidate_id (新 ID ≠ 旧 ID)
```

**v0.6c 退出标准**:
- [ ] 4 新测试全 pass
- [ ] v0.6b 4 + v0.6a 6 + v0.5b 5 + v0.4d 4 = 19/19 仍 pass
- [ ] mcp-resume list_tools 仍 6 工具 (retry_raw_resume 加参数, 工具数不变)
- [ ] health-check 14/14

**回滚**: revert 1 commit, force 参数移除, _handle_retry_raw_resume 恢复 v0.5b 行为

### v0.6a + v0.6b + v0.6c 总计: 3.5d

### v0.7 — skill_mgr 5 工具 (2d, 2 commit) — **可与 v0.6a 并行**

> 决策点 4-6: `list_skills` 复数 / 进程内单例 / list_tools 动态过滤

**目标**: skill 工具从 1 (install_skill_from_url) 扩到 5 (4 新 + 1 已有整合)。

```
apps/api/app/skills/_registry.py (新):
  - scan_skills() 扫 app/skills/*/skill.py, 解析 SKILL.md + name
  - load_state() 读 .omo/skill_state.json (gitignore)
  - save_state() 写 .omo/skill_state.json
  - get_enabled_tools() 返 list of (skill_name, tool_name, handler)

apps/api/app/tools/skill_tool.py:
  - 删 docstring 假承诺 4 个 (实际只有 handle_install_skill_from_url 1 个)
  - 新增 4 handler:
    - handle_list_skills(filter: str = "all") -> {enabled: [...], disabled: [...]}
    - handle_get_skill_info(name: str) -> {name, description, tools, version, enabled}
    - handle_enable_skill(name: str) -> {ok, name, enabled: true}
    - handle_disable_skill(name: str) -> {ok, name, enabled: false}
  - tools 列表 1 → 5 (新 4 工具定义)
  - handlers dict 1 → 5
  - list_tools 走 registry.get_enabled_tools() 动态过滤 (选项 α)

apps/api/app/tools/metadata.py:
  - list_skills: read, no role
  - get_skill_info: read, no role
  - enable_skill: admin, requires_role=admin
  - disable_skill: admin, requires_role=admin

.gitignore:
  - 加 .omo/skill_state.json

apps/api/tests/mcp/integration/test_skill_mgr_v0_7.py (新):
  - test_list_skills_returns_enabled_and_disabled_separately
  - test_list_skills_filter_enabled_only
  - test_get_skill_info_returns_metadata_or_NOT_FOUND
  - test_enable_skill_persists_to_state_json
  - test_disable_skill_persists_to_state_json
  - test_disable_skill_makes_tools_invisible_in_list_tools
  - test_enable_skill_makes_tools_visible_again
  - test_enable_skill_idempotent
```

**v0.7 退出标准**:
- [ ] 8 新测试全 pass
- [ ] mcp-skill-mgr list_tools 返回 5 工具 (4 新 + install_skill_from_url)
- [ ] disable 后 list_tools 看不到该 skill 的工具 (动态过滤生效)
- [ ] enable/disable 持久化 (重启 dev server 仍生效)
- [ ] e2e 14/14 + health-check 14/14

**回滚**: revert 2 commit + .gitignore 删 1 行

### v0.8 — 14 server 并行 spawn 压测 (1d, 1 commit)

> 决策点 7-8: 主目标=安全验证, 3 场景 stagger 0/100ms/1s, 10 trial

**目标**: 验证 14 server 并行 spawn 不死 (主), 找 fd/memory 瓶颈 (次)。

```
scripts/mcp_v4_v0_8_parallel_14_servers.py (新):
  - 并行 spawn 14 server (asyncio.gather)
  - 3 场景 × 10 trial = 30 次实验
  - 场景 A: 14 server 同时 spawn (stagger=0)
  - 场景 B: 14 server stagger 100ms
  - 场景 C: 14 server stagger 1s
  - 测得: spawn 耗时 / init 耗时 / list_tools 耗时 / 失败率 / RSS peak / open fd peak
  - 用 lsof -p <pid> | wc -l 测 fd
  - 用 ps -o rss= -p <pid> 测 memory
  - 不做函数级 profiling (推 v0.8.1)

docs/mcp-v4-v0.8-load-test-report.md (新):
  - 4 关键数字: P95 total / mean RSS / max fd / 失败率
  - 与 v0.4e 顺序测 (9208ms total, P95 898ms/server) 对比
  - 如 OOM/fd 超限: 写 ADR + 推 v0.8.1 修复
  - 如无问题: 写"14 server 并行安全, 无需再优化"
```

**v0.8 退出标准**:
- [ ] 30 次实验跑完 (3 场景 × 10 trial)
- [ ] 报告 4 关键数字 + 与 v0.4e 对比
- [ ] e2e 14/14 + health-check 14/14 (验证压测后系统仍健康)
- [ ] v0.8 ship report (即使只是"测了, 无问题, ship 文档")

**回滚**: revert 1 commit (删脚本 + 报告)

### v1.0a — .env 整合 (0.5d, 1 commit)

> 决策点 9: CI check 强制 (非可选)

**目标**: 完善 .env.example, 加 CI check 守门。

```
apps/api/.env.example:
  - 审视: 列 os.getenv() 读取的所有 key
  - 补缺 (POSTGRES_PASSWORD 占位 / QDRANT_API_KEY 可选 / LLM_PROVIDER=omlx 默认 等)
  - 重复/废弃项清理

apps/web/.env.example:
  - 审视: NEXT_PUBLIC_API_URL / NEXT_PUBLIC_WS_URL / NEXT_PUBLIC_*
  - 补缺

README.md:
  - 改"## 快速开始"引用路径: "实际密钥在 apps/api/.env (gitignore), 参考 apps/api/.env.example"

scripts/check_env_keys.py (新):
  - 扫 apps/api/app/ + apps/web/ 的 os.getenv / os.environ / Settings() 读取的 KEY
  - 与 apps/api/.env.example + apps/web/.env.example 匹配
  - 缺 key: WARN + 列表 (部署前必填)
  - 入口: scripts/check_env_keys.py --check
  - 接 .pre-commit-config.yaml (commit 前守门)
```

**v1.0a 退出标准**:
- [ ] apps/api/.env.example 完整 (覆盖所有 process_env 读取)
- [ ] apps/web/.env.example 完整
- [ ] README.md 引用路径正确
- [ ] scripts/check_env_keys.py 跑过: 0 缺 key
- [ ] health-check 14/14 (验证 .env 不破坏启动)

**回滚**: revert 1 commit (改 .env.example + README + check_env_keys.py)

### v1.0b — datetime.utcnow 修复 (0.5d, 1 commit)

> 决策点 10: `from datetime import UTC` (Python 3.11+ 风格)

**目标**: 4 文件 `datetime.utcnow()` → `datetime.now(UTC)`, 行为等价。

```
apps/api/app/core/rate_limit.py:
  - from datetime import UTC
  - datetime.utcnow() → datetime.now(UTC)
apps/api/app/services/support.py: 同上
apps/api/app/api/privacy.py: 同上
apps/api/app/api/csm.py: 同上

apps/api/tests/test_datetime_v1_0b_utc.py (新):
  - test_rate_limit_timestamp_is_tz_aware
  - test_privacy_timestamp_is_tz_aware
  - test_csm_timestamp_is_tz_aware
  - test_support_timestamp_is_tz_aware
```

**v1.0b 退出标准**:
- [ ] grep `utcnow` apps/api/app/ 0 命中
- [ ] 4 文件 import UTC
- [ ] 现有 rate_limit / privacy / csm 测试全 pass
- [ ] 4 新测试 pass
- [ ] health-check 14/14

**回滚**: revert 1 commit

## 5. 总时间表

| 阶段 | 估时 | 累计 (串行) | 累计 (并行优化) | 备注 |
|---|---|---|---|---|
| v0.6a RQ + submit/poll | 1.5d | 1.5d | 1.5d | RQ 轻, 1 worker |
| v0.6b WebSocket 推送 | 1d | 2.5d | 2.5d | 串行依赖 v0.6a |
| v0.6c force=True | 1d | 3.5d | 3.5d | 串行依赖 v0.6a (共享 _handle_retry_raw_resume) |
| **v0.7 skill_mgr 5 工具** | 2d | 5.5d | **3.5d** (与 v0.6a 并行) | 文件 0 重叠 |
| v0.8 14 server 并行压测 | 1d | 6.5d | 4.5d | 串行 (观察性, 不影响代码) |
| v1.0a .env 整合 | 0.5d | 7d | 5d | |
| v1.0b datetime 修复 | 0.5d | 7.5d | 5.5d | |
| **总计** | — | **7.5d 串行** | **5.5d 并行** | vs v0.5 系列 1.5d, v0.6 占 50% |

**关键并行机会**:
- v0.6a + v0.7 派 2 agent 同时干 (3.5d 总 vs 5.5d 串行) — **节省 2d**
- v0.6b/c + v0.8 + v1.0a/b 都是小改动, 串行即可

## 6. 不在 v0.6-v1.0 范围 (明确不做)

按 Momus 6.4 + v0.5-replan §5:

- ❌ **v0.6 batch_parse_resumes_async** (推 v0.7+ 后续, 单 async 优先)
- ❌ **v0.6 前端 WS 客户端** (前端 PR 后续, 跨栈)
- ❌ **v0.7 跨进程 registry 共享** (单 server 模式不需要)
- ❌ **v0.8 函数级 profiling** (推 v0.8.1, 仅当有真瓶颈)
- ❌ **v1.0a 删 root .env.example** (无 root .env.example, 不可行)
- ❌ **v1.0b 改 SQLAlchemy 列类型** (utcnow 改 aware 不需要 DB schema 改)
- ❌ **v0.9 candidate_search 归位** (条件触发, 看负载)
- ❌ **v1.1 Phase D E2E live API** (需 dev 栈稳定)
- ❌ **v1.2 Phase E CI/CD** (推后续)
- ❌ **重写 OMLXClient** (CLAUDE.md 强制)
- ❌ **多租户** (明确不做)
- ❌ **前端技术栈替换**

## 7. 后续路线 (v2.0+ 候选)

| 阶段 | 内容 | 估时 |
|---|---|---|
| v0.6.1 | batch_parse_resumes_async + 多 task 进度合并 | 1.5d |
| v0.8.1 | 14 server 并行 profiling (如 v0.8 报告有 OOM) | 1d |
| v0.9 | candidate_search 归位 (看负载, 拆 mcp-candidate-search) | 1-2d |
| v1.1 | Phase D E2E live API 全跑 (需 dev 栈) | 1d |
| v1.2 | Phase E CI/CD 强化 (L1 守门 + L2 audit + L3 集成测) | 2d |
| v2.0 | mcp-interview/mcp-evaluation Bheavy 异步化 (借鉴 v0.6) | 4d |

## 8. 回滚方法总览

```bash
# v0.6a + v0.6b + v0.6c 共 4 commit 在 main (按 v0.4 + v0.5 模式打 tag)
git tag -l "mcp-v4-v0.6*"
# v0.6a 后打: mcp-v4-v0.6a-pre + mcp-v4-v0.6a-shipped
# v0.6b 后打: mcp-v4-v0.6b-pre + mcp-v4-v0.6b-shipped
# v0.6c 后打: mcp-v4-v0.6c-pre + mcp-v4-v0.6c-shipped

# 失败回滚
git checkout mcp-v4-v0.6a-pre
# v0.6a 改 5 文件: parse_worker.py + parse_task.py + raw_resume.py + resume_parser.py + docker-compose
# 回滚 = revert 2 commit + 删 docker-compose 2 service
# v0.6b 改 1 文件: raw_resume.py (WS 端点) + parse_task.py (进度推送)
# 回滚 = revert 1 commit
# v0.6c 改 1 文件: resume_parser.py (force 参数)
# 回滚 = revert 1 commit

# v0.7 共 2 commit
git checkout mcp-v4-v0.7-pre
# 改 3 文件: _registry.py + skill_tool.py + metadata.py + .gitignore
# 回滚 = revert 2 commit

# v0.8 共 1 commit (脚本+报告)
git checkout mcp-v4-v0.8-pre
# 改 1 文件: scripts/mcp_v4_v0_8_parallel_14_servers.py + docs/mcp-v4-v0.8-load-test-report.md
# 回滚 = revert 1 commit

# v1.0a + v1.0b 共 2 commit
git checkout mcp-v4-v1_0a-pre
# 改 3 文件: apps/api/.env.example + apps/web/.env.example + scripts/check_env_keys.py + README.md
# 回滚 = revert 1 commit
git checkout mcp-v4-v1_0b-pre
# 改 4 文件: rate_limit.py + support.py + privacy.py + csm.py
# 回滚 = revert 1 commit
```

## 9. Momus 审查 (修正 30 项)

draft plan 找出的 30 项 gap, 全部在修正版处理:

| # | 问题 | draft 章节 | 修正版章节 |
|---|---|---|---|
| 1 | v0.5-replan §6 错误归因 (C.2/C.6 已 ship) | momus §0.1 | §1 状态审计 |
| 2 | v0.6 单 PR 过粗 (3-4d) | momus §0.2 | §4 拆 v0.6a/b/c |
| 3 | v0.6 + v0.7 串行过长 | momus §0.3 | §5 并行 2 agent 优化 |
| 4 | force=True 4 方案未明 | momus §1.1 | §3 决策点 3 + §4 v0.6c |
| 5 | Celery vs RQ 未选 | momus §1.2 | §3 决策点 1 + §4 v0.6a (RQ) |
| 6 | WebSocket 鉴权未明 | momus §1.3 | §3 决策点 2 + §4 v0.6b (?token=) |
| 7 | 旧 3 同步工具去留 | momus §1.4 | §4 v0.6a (保留 + deprecated) |
| 8 | 5 测试不够, Celery 异常路径 | momus §1.5 | §4 v0.6a (加 3 异常测试) |
| 9 | rollback 计划缺 | momus §1.6 | §8 回滚总览 + §4 各 PR 退出 |
| 10 | v0.5b retry 兼容性 | momus §1.7 | §4 v0.6c (force 默认 False) |
| 11 | 估时偏低 | momus §1.8 | §5 总时间表 (3.5d v0.6) |
| 12 | _do_extract_and_link 适配 | momus §1.9 | §4 v0.6c (handler 层做清空) |
| 13 | batch_parse_resumes 异步化 | momus §1.10 | §6 推 v0.6.1 |
| 14 | Celery worker watchdog | momus §1.11 | §4 v0.6a (make celery:watch) |
| 15 | skill_tool docstring 与 §7.4 矛盾 | momus §2.1 | §3 决策点 4 + §4 v0.7 (list_skills 复数) |
| 16 | registry 跨进程 vs 单例 | momus §2.2 | §3 决策点 5 + §4 v0.7 (单例) |
| 17 | enable/disable 实现 | momus §2.3 | §3 决策点 6 + §4 v0.7 (选项 α) |
| 18 | 4 个未注册 handler 虚假承诺 | momus §2.4 | §4 v0.7 (删 docstring 假承诺) |
| 19 | .omo/skill_state.json 路径 | momus §2.5 | §4 v0.7 (.gitignore) |
| 20 | v0.7 估时偏低 | momus §2.6 | §5 (2d) |
| 21 | v0.7 与 v0.6 并行 | momus §2.7 | §5 (并行 2 agent) |
| 22 | 1 trial × 3 场景数字不可信 | momus §3.1 | §4 v0.8 (10 trial) |
| 23 | dev 机非 prod 价值低 | momus §3.2 | §3 决策点 7 + §4 v0.8 (安全验证) |
| 24 | 场景 B 100ms 设计依据 | momus §3.3 | §3 决策点 8 + §4 v0.8 (0/100ms/1s) |
| 25 | cProfile 复杂 | momus §3.4 | §4 v0.8 (不做 profiling) |
| 26 | v0.8 估时 | momus §3.5 | §5 (1d) |
| 27 | v1.0a "审视 .env.example" 缺验证 | momus §4.1 | §4 v1.0a (check_env_keys.py 强制) |
| 28 | apps/web/.env.example 内容核实 | momus §4.2 | §4 v1.0a (读现状第一) |
| 29 | docker-compose 引用检查 | momus §4.3 | §4 v1.0a (核实) |
| 30 | v1.0b datetime naive vs aware 差异 | momus §5.1 | §4 v1.0b (4 测验证 tz-aware) |

**未采纳的 Momus 建议** (讨论后不采纳):
- ~~v0.6 batch_parse_resumes_async 一起做~~ (单 async 优先, 推 v0.6.1)
- ~~v0.7 跨进程 registry~~ (单 server 模式不需要)
- ~~v0.8 profiling 一起做~~ (推 v0.8.1, 仅当有真瓶颈)
- ~~v1.0a 删 root .env.example~~ (无 root, 不可行)

## 10. 引用

- v0.6-v1.0 draft: `.omo/plans/v0.6-plus-draft.md`
- Momus 审核: `.omo/plans/v0.6-plus-momus-review.md`
- v0.5-replan 风格参照: `.omo/plans/v0.5-replan.md`
- v0.5 ship reports: `docs/mcp-v4-v0.5{a,b}-ship-report.md`
- Phase C 原始: `.omo/plans/completion-plan-v5.md` §C.1-C.6
- LLM retry 实际: `apps/api/app/llm/retry.py` + commit `782a3a9`
- session progress 2026-06-02: `.omo/plans/session-progress-2026-06-02.md`
- v0.5 Momus 修 6 项: `.omo/plans/v0.5-replan.md` §8
