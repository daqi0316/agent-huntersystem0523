# Phase MCP v4 后续 v0.6+ 重规划 — Momus 审核 + 修正

> **重规划时机**: v0.5a (commit 88066a3) + v0.5b (commit efcd0a4) ship 后, raw_resumes retry 工具闭环。
> **依据**: `.omo/plans/v0.5-replan.md` §6 v0.6+ 候选 + Phase C 实际状态审计 (2026-06-07)。
> **关键修正**: v0.5-replan 写"v1.0: Phase C 4 项 (C.2/C.3/C.5/C.6)"实际**只有 C.3 + C.5 真未做**——C.2/C.6 已 ship, C.4 错列。

## 0. Phase C 实际状态审计（Momus 第 1 个 gap）

`.omo/plans/completion-plan-v5.md` 原始 Phase C 6 项 + `.omo/plans/session-progress-2026-06-02.md` 实际状态对照:

| 项 | 描述 | 实际状态 | 证据 |
|---|---|---|---|
| C.1 限流 | FastAPI middleware | ✅ ship | health-check step 8 60 并发 33× 429 |
| C.2 LLM retry | `app/llm/retry.py` 1s→16s 5 retries | ✅ **ship** | commit `782a3a9 feat(phase-z): rate limiting, LLM retry, test fixes, 83% coverage` |
| C.3 .env 整合 | 删 root .env.example | ⚠️ **不可行** | 根目录**无** .env.example（apps/api/.env + apps/api/.env.example + apps/web/.env.example + apps/web/.env.local 4 个）。需重新定义"整合" |
| C.4 Python 版本 | 3.13→3.14 | ✅ ship | `apps/api/Dockerfile` + `Dockerfile.dev` 均 `FROM python:3.14-slim-bookworm` |
| C.5 datetime utcnow | 4 文件含 `datetime.utcnow()` | ⚠️ **未做** | grep 命中 rate_limit.py / support.py / privacy.py / csm.py |
| C.6 docker healthcheck | web + 验证 4 基础设施 | ✅ ship | docker-compose.dev.yml 行 116-121 web healthcheck + 行 13/27/41/59/91 postgres/redis/qdrant/minio/rabbitmq |

**v0.5-replan §6 错误归因**: 把"v1.0: Phase C 4 项"写成 C.2 + C.3 + C.5 + C.6 4 项, 实际只 C.3 + C.5 真未做。

---

## 1. v0.6 Draft Plan — mcp-resume Bheavy 完整版 (异步 LLM + WebSocket) + force=True

### 1.1 范围（draft）

```
apps/api/app/tools/resume_parser.py:
  - parse_resume 拆为 submit + poll 两步:
    - submit: 落 raw_resumes (status=processing) + 创建 Celery/RQ 任务 → 返 task_id
    - poll: GET /raw_resumes/{id}/status → 返 status + (parsed 时的 result)
  - retry_raw_resume 同样拆 submit + poll
  - WebSocket /raw_resumes/{id}/progress 推送状态变化 (processing→parsed/failed)

apps/api/app/mcp_servers/builtin/resume_server.py:
  - tools 列表: parse_resume_async / poll_parse_resume / retry_raw_resume_async
    旧 3 工具 (sync) 保留 → 4 工具 (sync) + 3 工具 (async) = 7
  - metadata: parse_resume_async retryable=True (LLM 5xx 抖)

apps/api/app/api/raw_resume.py (新):
  - GET /raw_resumes/{id}/status
  - WS /raw_resumes/{id}/progress
  - POST /raw_resumes/{id}/retry (HTTP 层入口, 给前端用)

apps/api/app/services/parse_task.py (新):
  - Celery/RQ task: 调 _do_extract_and_link (v0.5a 抽出)
  - 失败 3 次写 raw_resumes.status=failed + error_message 落库

apps/api/app/workers/parse_worker.py (新):
  - Celery worker 进程入口 (apps/api/.venv/bin/python -m app.workers.parse_worker)
  - 部署: docker-compose.dev.yml + prod.yml 加 celery service
  - 队列: Redis (已有)

apps/api/tests/mcp/integration/test_resume_parser_v0_6_async.py (新):
  - test_submit_returns_task_id_and_raw_resume_id
  - test_poll_status_processing/parsed/failed 三态
  - test_websocket_progress_pushes_terminal_status
  - test_async_retry_failed_resume_via_task
  - test_force_true_overwrites_existing_candidate (force=True 主路径)
  - test_force_false_creates_new_candidate (默认行为)
  - test_async_llm_failure_keeps_status_failed (LLM 3 次失败路径)

scripts/mcp_v4_e2e_14_servers.py:
  - mcp-resume tools 4 → 7 (sync 保留 + async 加 3)
```

### 1.2 退出标准

- [ ] submit/poll/WS 三端点端到端跑通 (Playwright + curl)
- [ ] Celery worker 起来, 任务真异步执行 (不阻塞 HTTP)
- [ ] 5 新测试全 pass
- [ ] v0.5b 5 测试 + v0.4d 4 测试 = 9/9 仍 pass
- [ ] mcp-resume list_tools 返回 7 工具
- [ ] e2e 14/14 仍 pass
- [ ] health-check 14/14
- [ ] v0.6 ship report

### 1.3 估时

3-4d (Celery 接入 + WS 推送 + force=True + 5 测试 + e2e + ship report)

---

## 2. v0.7 Draft Plan — skill_mgr 工具扩展 (list/get/enable/disable 4 工具)

### 2.1 范围（draft）

```
apps/api/app/skills/_registry.py (新):
  - 单实例 source of truth: 扫 app/skills/*/skill.py 解析 SKILL.md + name
  - 启动时加载 + enable/disable 状态持久化到 .omo/skill_state.json (本地)
  - 4 工具基于 registry 实现 (而不是散在 skill_tool.py 的 5 个未注册函数)

apps/api/app/tools/skill_tool.py:
  - 5 现有 handler (handle_install_skill_from_url 等) 重新审视
  - 新增 4 handler:
    - handle_list_skills(filter: str = "all") -> {enabled: [...], disabled: [...]}
    - handle_get_skill_info(name: str) -> {name, description, tools, enabled, version}
    - handle_enable_skill(name: str) -> {name, enabled: true}
    - handle_disable_skill(name: str) -> {name, enabled: false}
  - tools 列表加 4 工具定义 (5 → 9)
  - handlers dict 加 4 映射 (1 → 5)
  - 旧 handle_install_skill / handle_list_skills (重名) / handle_install_gallery_skill / handle_list_gallery_skills 评估:
    - handle_install_skill (代码内已有) → 整合进 install_skill_from_url (扩展参数)
    - handle_list_gallery_skills → 整合进 list_skills (filter=gallery)
    - handle_install_gallery_skill → 整合进 install_skill_from_url (filter=gallery)

apps/api/app/tools/metadata.py:
  - 注册 4 工具元数据
  - list_skills: read capability, 无需 role
  - enable_skill / disable_skill: admin capability, requires_role=admin
  - get_skill_info: read capability

apps/api/tests/mcp/integration/test_skill_mgr_v0_7.py (新):
  - test_list_skills_returns_enabled_and_disabled
  - test_list_skills_filter_gallery_returns_only_gallery
  - test_get_skill_info_returns_metadata
  - test_get_skill_info_nonexistent_returns_NOT_FOUND
  - test_enable_skill_updates_state_and_persists
  - test_disable_skill_updates_state_and_persists
  - test_enable_already_enabled_is_idempotent
  - test_disable_skill_runtime_tools_does_not_affect (验证 enable/disable 不杀进程)
```

### 2.2 退出标准

- [ ] 4 工具在 mcp-skill-mgr list_tools 返回
- [ ] 8 新测试全 pass
- [ ] registry 单实例 source of truth, 4 工具读同一 state
- [ ] enable/disable 持久化 (重启 dev server 仍生效)
- [ ] health-check 14/14
- [ ] v0.7 ship report

### 2.3 估时

1.5-2d (registry + 4 handler + 8 测试 + ship report)

---

## 3. v0.8 Draft Plan — 14 server 并行 spawn 压测 + profiling

### 3.1 范围（draft）

```
scripts/mcp_v4_v0_8_parallel_14_servers.py (新):
  - 并行 spawn 14 server (asyncio.gather)
  - 测 3 场景:
    - 场景 A: 14 server 同时 spawn (无 stagger) — 找 fd/memory 瓶颈
    - 场景 B: 14 server stagger 100ms spawn — 模拟 v0.4c secondary phase 30s 触发
    - 场景 C: 14 server stagger 1s spawn — 模拟 prod 滚动重启
  - 每场景 5 trial
  - 测得: spawn 耗时 / init 耗时 / list_tools 耗时 / 失败率 / RSS peak / open fd peak
  - 与 v0.4e 14 server 顺序 (9208ms total, P95 898ms/server) 对比

scripts/profile_mcp_servers.py (新, 可选):
  - cProfile / py-spy 接 14 server spawn
  - 输出: top 20 函数耗时 (init / list_tools / shutdown)
  - 找: 慢在 import? DB connect? MCP 协议握手?

docs/mcp-v4-v0.8-load-test-report.md (新):
  - 报告: P95 数字 + 瓶颈分析 + 优化建议
  - 如有 fd/memory OOM, 写 ADR + 推 v0.8.1 修复
  - 如无问题, 写"14 server 并行 spawn 安全, 无需再优化"
```

### 3.2 退出标准

- [ ] 14 server 并行 spawn 5 trial × 3 场景 跑完
- [ ] 报告里 4 关键数字: P95 total / mean RSS / max fd / 失败率
- [ ] 与 v0.4e 顺序测数字对比
- [ ] 失败场景 (OOM / fd 超限) 写 ADR + 推 v0.8.1
- [ ] 通过: e2e 14/14 + health-check 14/14 仍 pass
- [ ] v0.8 ship report (即使只是"测了, 无问题, ship 文档")

### 3.3 估时

0.5-1d (脚本 + 3 场景 + 报告)。如有修复 1-2d。

---

## 4. v1.0 Draft Plan — Phase C 剩余 (C.3 + C.5)

### 4.1 v1.0a — .env 整合 (C.3)

> **重要**: v0.5-replan 描述"删 root .env.example"实际不可行 (根目录无 .env.example)。重新定义:

```
根目录现状 (find .env* 验证):
  apps/api/.env           (gitignore, 实际密钥)
  apps/api/.env.example   (committed, 48 行)
  apps/web/.env.example   (committed, 7 行)
  apps/web/.env.local     (gitignore, 实际密钥)

v1.0a 实际工作:
  1. apps/api/.env.example 审视:
     - 列项是否完整 (DB/REDIS/QDRANT/MINIO/LLM/CORS/...)
     - 重复/废弃项清理
     - 缺 key 加注释 (← 占位值, 部署时必填)
  2. apps/web/.env.example 审视:
     - NEXT_PUBLIC_* 完整
     - 缺 key 加注释
  3. README.md 引用:
     - 删根目录"## 快速开始"里 apps/api/.env.example 创建说明
     - 加: "实际密钥在 apps/api/.env (gitignore), 参考 apps/api/.env.example"
  4. CI check (可选):
     - .github/workflows/*.yml 加 step: 验证 .env.example 含必填 KEY
     - fail 时提示 "缺 KEY_XXX 在 apps/api/.env.example"
```

#### 4.1.1 退出标准

- [ ] apps/api/.env.example 完整 (覆盖所有 process_env 读取的 key)
- [ ] apps/web/.env.example 完整
- [ ] README.md 引用 .env.example 路径正确
- [ ] health-check 14/14 仍 pass (验证 .env 不破坏启动)

#### 4.1.2 估时

0.5d

### 4.2 v1.0b — datetime.utcnow 修复 (C.5)

```
4 文件含 datetime.utcnow() (grep 验证):
  apps/api/app/core/rate_limit.py
  apps/api/app/services/support.py
  apps/api/app/api/privacy.py
  apps/api/app/api/csm.py

v1.0b 实际工作:
  - 全部 datetime.utcnow() → datetime.now(datetime.UTC)
  - 顶部加 from datetime import UTC (Python 3.11+ 才有 UTC 单例)
  - 行为等价 (UTC naive → UTC aware)
  - 验证现有测试 (rate_limit_test / privacy_test / csm_test) 仍过
  - 加 1 测试: 验证 timestamp 字段是 tz-aware
```

#### 4.2.1 退出标准

- [ ] grep `utcnow` apps/api/app/ 无命中
- [ ] 4 文件 import datetime 加 UTC
- [ ] 现有测试 (rate_limit / privacy / csm) 全 pass
- [ ] 新 1 测试 pass

#### 4.2.2 估时

0.5d

### 4.3 v1.0 总计

1d (v1.0a 0.5d + v1.0b 0.5d)

---

## 5. v0.6-v1.0 总时间表

| 阶段 | 估时 | 累计 | 备注 |
|---|---|---|---|
| v0.6 mcp-resume Bheavy + force=True | 3-4d | 3-4d | 最大块, 涉及 Celery + WS 部署 |
| v0.7 skill_mgr 4 工具 | 1.5-2d | 5-6d | |
| v0.8 14 server 并行 spawn 压测 | 0.5-1d | 6-7d | |
| v1.0a .env 整合 | 0.5d | 6.5-7.5d | |
| v1.0b datetime 修复 | 0.5d | 7-8d | |
| **总计** | **6-8d** | — | vs v0.5 系列 1.5d, v0.6 单 PR 占 50% |

---

## 6. 不在 v0.6-v1.0 范围

- ❌ v0.9 candidate_search 归位 (按 v0.5-replan 描述"看负载决定", 条件触发, 不在 backlog)
- ❌ v1.1 Phase D E2E live API 全跑 (需 dev 栈稳定 + 14 server 自动启停)
- ❌ v1.2 Phase E CI/CD 强化
- ❌ mcp-resume Bheavy 之外的其它 server 异步化 (interview/evaluation 也 Bheavy, 但 v0.6 暂只 resume)

---

## 7. 引用

- v0.5-replan: `.omo/plans/v0.5-replan.md` §6 v0.6+ 候选
- v0.5a ship: `docs/mcp-v4-v0.5a-ship-report.md`
- v0.5b ship: `docs/mcp-v4-v0.5b-ship-report.md`
- Phase C 原始: `.omo/plans/completion-plan-v5.md` §C.1-C.6
- LLM retry 实际: `apps/api/app/llm/retry.py` + commit `782a3a9`
- session progress 2026-06-02: `.omo/plans/session-progress-2026-06-02.md`
