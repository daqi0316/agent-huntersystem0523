# Session Handoff — 2026-05-23

## Current State (Ready to Continue)

| Service | Port | Status |
|---------|------|--------|
| PostgreSQL | 5432 | ✅ docker (healthy) |
| Redis | 6379 | ✅ docker (healthy) |
| Qdrant | 6333 | ✅ docker |
| omlx (Qwen3.6) | **8000** | ✅ auto-running (OpenAI-compatible) |
| API Server | — | ❌ not started |
| Frontend | 3000 | ❌ not started |

## Already Fixed
- `apps/api/app/core/config.py`:
  - `llm_model`: `qwen3.6` → `Qwen3.6-35B-A3B-4bit`
  - `llm_embed_model`: `bge-m3` → `bge-m3-mlx-4bit`
  - `llm_base_url`: `http://localhost:8001/v1` → `http://localhost:8000/v1`

## Next Steps (Exact Commands)

### 1. Install deps & start API
```bash
cd /Users/qixia/agent-huntersystem0523/apps/api
uv pip install "fastapi>=0.111.0" "uvicorn[standard]>=0.30.0" "sqlalchemy[asyncio]>=2.0.30" "asyncpg>=0.29.0" "alembic>=1.13.0" "pydantic>=2.6.0" "pydantic-settings>=2.2.0" "redis>=5.0.0" "qdrant-client>=1.9.0" "python-jose[cryptography]>=3.3.0" "passlib[bcrypt]>=1.7.4" "python-multipart>=0.0.9" "httpx>=0.27.0" "openai>=1.12.0" "prometheus-client>=0.20.0" "minio>=7.2.0"
nohup uv run --no-sync python -m uvicorn app.main:app --host 0.0.0.0 --port 8888 > /tmp/api-server.log 2>&1 &
```

### 2. Run migration + seed
```bash
cd apps/api
uv run --no-sync alembic upgrade head
# Seed test user: e2e-tester@test.com / E2ePass123!
```

### 3. Validate LLM endpoints
Test chat: `POST /api/v1/agent/chat`
Test JD gen: `POST /api/v1/loop/jd-generate`
Test screening: `POST /api/v1/pipeline/screen-resume`
Test multi-evaluate: `POST /api/v1/parallel/multi-evaluate`
Test RAG: `POST /api/v1/agent/knowledge-query`
Test human-loop: `POST /api/v1/human-loop/schedule`

### 4. Start frontend
```bash
cd apps/web
pnpm dev
```

## LLM Integration Status
| Endpoint | Agent Type | LLM? | Status |
|----------|-----------|-------|--------|
| `POST /agent/chat` | 图1 Single | ✅ chat() | Config fixed, ready |
| `POST /agent/generate-jd` | 图1 Single | ✅ GenEvalLoop | Ready |
| `POST /agent/knowledge-query` | 图1 Single | ✅ chat+embed | Ready |
| `POST /pipeline/screen-resume` | 图2 Pipeline | ✅ parse/match/gate | Ready |
| `POST /router/classify` | 图3 Router | ❌ stub | Low priority |
| `POST /parallel/multi-evaluate` | 图4 Aggregator | ✅ chat() | Ready |
| `POST /orchestrator/analyze` | 图5 Orchestrator | ❌ stub | Low priority |
| `POST /loop/jd-generate` | 图6 GenEval | ✅ LLM calls | Ready |
| `POST /human-loop/schedule` | 图7 Human-in-Loop | ✅ chat() | Ready |

## Key Context
- omlx auto-starts on port 8000 (GUI app runs `omlx-cli serve --port 8000`), **don't fight it**
- API on 8888 avoids port conflict
- The `pyproject.toml` build issue (setuptools flat-layout sees `app/` + `alembic/`) is worked around by using `uv pip install` + `uv run --no-sync` — no editable build needed
- Test user already in DB from previous migration runs
