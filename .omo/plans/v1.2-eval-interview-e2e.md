# v1.2 Plan — 跨 Server 业务流 E2E (evaluation + interview)

> **日期**: 2026-06-07
> **依据**: v0.8.2 ship report §7 (v1.2 1d)
> **目标**: 跨 3 server E2E (candidate → job → interview → evaluation → report)

## 1. 业务流定义 (5 步)

| Step | Server | Tool | Mock |
|---|---|---|---|
| 1 创建 candidate | mcp-candidate | (v1.1 E2E 复用) | mock LLM |
| 2 创建 job | mcp-job | `create_job` | 无 (走 DB) |
| 3 安排面试 | mcp-interview | `schedule_interview` | 无 (走 DB) |
| 4 保存评估 | mcp-evaluation | `save_evaluation` | 无 (走 DB) |
| 5 生成报告 | mcp-evaluation | `generate_evaluation_report` | 无 (走 DB) |

**依赖关系**: step 1 → step 2 (并行) → step 3 → step 4 → step 5

## 2. 文件清单

### 2.1 新文件 (1 个)

`apps/api/tests/mcp/integration/test_e2e_evaluation_interview_v1_2.py` (1 E2E, 5 step)
- 主路径: 完整 5 步
- 估时 0.5d (1 E2E + 文档)

### 2.2 改文件 (0 个)

- 不动 production code
- 验证跨 server 业务流在真 DB 路径下工作

## 3. E2E 设计

```python
async def test_e2e_candidate_interview_evaluation(client):
    # Step 1: 创建 candidate (复用 v1.1 模式: HTTP upload + parse_resume with mock LLM)
    files = {"file": ("r.txt", RESUME_TEXT.encode(), "text/plain")}
    r = await client.post("/api/v1/resume/upload-resume", files=files)
    plain_text = r.json()["plain_text"]
    
    with patch("app.tools.resume_parser.extract_from_text", new_callable=AsyncMock) as mock_extract:
        mock_extract.return_value = EXTRACTED
        parse_result = await resume_handlers["parse_resume"](content=plain_text)
    candidate_id = parse_result["data"]["candidate_id"]
    
    # Step 2: 创建 job (用 job_handlers)
    job_result = await job_handlers["create_job"](
        title="Senior Python Engineer",
        description="Backend dev with FastAPI experience",
        ...
    )
    job_id = job_result["id"]
    
    # Step 3: 安排面试
    interview_result = await interview_handlers["schedule_interview"](
        candidate_id=candidate_id,
        job_id=job_id,
        scheduled_time="2026-07-01T10:00:00+08:00",
        notes="视频面试 R1",
    )
    interview_id = interview_result["id"]
    assert interview_result["status"] == "scheduled"
    
    # Step 4: 保存评估
    eval_result = await evaluation_handlers["save_evaluation"](
        interview_id=interview_id,
        round="R1",
        overall_score=8.5,
        verdict="strong_hire",
        dimensions={"技术": 9, "沟通": 8},
        key_observations="扎实的 FastAPI 经验, 系统设计能力强",
        feedback="强烈推荐",
    )
    assert eval_result["status"] == "success"
    
    # Step 5: 生成报告
    report_result = await evaluation_handlers["generate_evaluation_report"](
        candidate_id=candidate_id,
    )
    assert report_result["status"] == "success"
    # 验证报告含 R1 评估
    assert report_result["data"]["rounds"]["R1"]["overall_score"] == 8.5
    assert report_result["data"]["rounds"]["R1"]["verdict"] == "strong_hire"
```

## 4. 验证 SOP

按 `docs/system-health-check.md`:
- 62 + 1 = 63 测 累计 pass
- health-check 13/14 (1 限流 known)

## 5. 不做 (Out of Scope)

| 不做 | 原因 |
|---|---|
| 测 multi-round interview (R1+R2+R3) | 推 v1.2.1 |
| 测 full pipeline orchestrator | 推 v1.3 (需 GraphState 重构) |
| 改 production code | v1.2 纯测试 |
| 删 health-check 限流 known issue | 推后续 |

## 6. 风险

| 风险 | 等级 | 缓解 |
|---|---|---|
| _handle_create_job 签名未知 | 低 | 读 tools/job.py 完整 body |
| EvaluationRound/Verdict enum 不匹配 | 低 | 已有 ValueError fallback (handler 内部) |
| report 字段结构未知 | 中 | 测 assert 只验关键字段, 不锁结构 |

## 7. 时间线 (1d)

| 阶段 | 工作 | 估时 |
|---|---|---|
| A | 查 _handle_create_job 完整签名 | 0.1d |
| B | 写 E2E test | 0.3d |
| C | 跑测试 + 修 bug | 0.2d |
| D | health-check + commit + ship report | 0.4d |

## 8. 引用

- v1.1 E2E 模板: `apps/api/tests/mcp/integration/test_e2e_phase_d_v1_1.py`
- 候选创建: `apps/api/app/tools/resume_parser.py:579` + `candidate.py:200`
- 面试安排: `apps/api/app/tools/interview.py:26-40`
- 评估保存: `apps/api/app/tools/evaluation.py:16-69`
- 报告生成: `apps/api/app/tools/evaluation.py:70-...`
- Job 创建: `apps/api/app/tools/job.py:123`
