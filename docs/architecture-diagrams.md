# AI Recruitment System — Architecture & Sequence Diagrams

> Generated by Sisyphus from `/Users/qixia/agent-huntersystem0523` on 2026-06-02
>
> **Stack**: FastAPI + LangGraph + Next.js 14 + PostgreSQL + Redis + Qdrant + MinIO
> **Coverage**: 90.43% · 2,014 tests passing

---

## 1. System Architecture (Bird's-Eye View)

```mermaid
flowchart TB
    subgraph Client["🖥️  Frontend (Next.js 14, apps/web)"]
        UI["App Router Pages<br/>(dashboard)/candidates,<br/>interview, screening, ..."]
        Comp["Components<br/>features/* + ui/*"]
        Store["Zustand Stores<br/>+ React Query"]
    end

    subgraph Gateway["🚪  API Gateway (FastAPI, apps/api)"]
        Middle["Middleware<br/>CORS · RateLimit<br/>Request Logging"]
        Router["27 API Routers<br/>under /api/v1/*"]
        Auth["Auth (JWT/HTTPBearer)"]
    end

    subgraph Agents["🧠  Agent Layer (LangGraph Orchestration)"]
        Orch["OrchestratorGraph<br/>StateGraph · checkpoint"]
        Router2["RouterAgent<br/>(intent classification)"]
        ResParser["ResumeParserGraph<br/>7-step workflow"]
        Spec["Specialist Agents<br/>screening · interview<br/>sourcing · offering<br/>onboarding · analytics"]
    end

    subgraph Services["⚙️  Service Layer (21 services)"]
        AgentSvc["AgentService<br/>(LLM retry, JSON parse)"]
        ApprovalSvc["ApprovalService<br/>(Human-in-loop)"]
        QdrantSvc["QdrantService<br/>(vector retrieval)"]
        OPSvc["OperationService<br/>(audit log)"]
    end

    subgraph Tools["🔧  Tools Layer (13+ builtin tools)"]
        ResumeT["resume_parser"]
        ScreenT["screening"]
        InterviewT["interview"]
        EvalT["evaluation"]
        KBT["knowledge"]
    end

    subgraph Infra["💾  Infrastructure"]
        PG[("PostgreSQL<br/>15+ models")]
        Redis[("Redis<br/>cache · rate limit<br/>graph state")]
        Qdrant[("Qdrant<br/>vector search")]
        Minio[("MinIO<br/>file storage")]
        LLM["OMLX / vLLM<br/>LLM inference"]
    end

    subgraph BG["⏰  Background Loops (asyncio)"]
        RecLoop["recommendation_scheduler_loop"]
        AggLoop["aggregation_loop<br/>(5min UPSERT)"]
    end

    UI <-->|REST + SSE| Gateway
    Comp --> Store
    Store -->|fetch / tRPC| Gateway
    Gateway --> Middle --> Auth --> Router

    Router -->|chat · agent| Orch
    Router -->|pipeline| ResParser
    Orch --> Router2
    Orch --> Spec
    Spec --> AgentSvc
    Spec --> Tools
    AgentSvc --> LLM

    Tools --> Services
    Services --> PG
    Services --> Redis
    Services --> Qdrant
    Services --> Minio
    QdrantSvc --> Qdrant
    OPSvc --> PG

    Auth --> ApprovalSvc
    ApprovalSvc --> Redis

    RecLoop --> PG
    RecLoop --> Redis
    AggLoop --> PG

    style Client fill:#e1f5ff,stroke:#0288d1
    style Gateway fill:#fff3e0,stroke:#f57c00
    style Agents fill:#f3e5f5,stroke:#7b1fa2
    style Services fill:#e8f5e9,stroke:#388e3c
    style Tools fill:#fce4ec,stroke:#c2185b
    style Infra fill:#f5f5f5,stroke:#616161
    style BG fill:#fff8e1,stroke:#ffa000
```

---

## 2. Backend Layered Architecture

```mermaid
flowchart LR
    L1["① HTTP Layer<br/>FastAPI Routers<br/>(27 files)"]
    L2["② Agent Layer<br/>BaseAgent subclasses<br/>(14+ agents)"]
    L3["③ Service Layer<br/>AsyncSession + business logic<br/>(21 services)"]
    L4["④ Tool Layer<br/>OpenAI function-calling schema<br/>(13+ tools)"]
    L5["⑤ Model Layer<br/>SQLAlchemy 2.0 ORM<br/>(15+ tables)"]
    L6["⑥ Infrastructure<br/>Postgres · Redis · Qdrant<br/>MinIO · LLM"]

    L1 -->|Depends / injects| L2
    L2 -->|calls .run()| L3
    L2 -->|invokes via LLM| L4
    L3 -->|CRUD| L5
    L3 -->|side effects| L6
    L4 -->|uses| L3
    L5 --> L6

    style L1 fill:#bbdefb
    style L2 fill:#ce93d8
    style L3 fill:#a5d6a7
    style L4 fill:#f48fb1
    style L5 fill:#ffcc80
    style L6 fill:#b0bec5
```

---

## 3. Orchestrator Graph — Single-Intent Flow

```mermaid
stateDiagram-v2
    [*] --> intent_recognition
    intent_recognition --> _decide_route

    state _decide_route <<choice>>
    _decide_route --> multi_stage_decompose: multi_stage=true
    _decide_route --> execute_resume_parser: intent=resume_parser
    _decide_route --> execute_screening: intent=screening
    _decide_route --> execute_interview: intent=interview
    _decide_route --> execute_sourcing: intent=sourcing | jd | candidate_search
    _decide_route --> execute_offering: intent=offering
    _decide_route --> execute_onboarding: intent=onboarding
    _decide_route --> execute_analytics: intent=analytics | report
    _decide_route --> END: chat | knowledge_query | settings

    execute_resume_parser --> END
    execute_screening --> END
    execute_interview --> END
    execute_sourcing --> END
    execute_offering --> END
    execute_onboarding --> END
    execute_analytics --> END
```

---

## 4. Orchestrator Graph — Multi-Stage DAG (PR-V.1)

```mermaid
flowchart TB
    Start([START]) --> IR[intent_recognition<br/>RouterAgent.is_multi_intent]
    IR -->|multi=True| MSD[multi_stage_decompose<br/>LLM JSON · build_dag]
    IR -->|single| Single["execute_*<br/>(see §3)"]
    Single --> End1([END])

    MSD -->|levels: List[List[int]]| EL[execute_level<br/>asyncio.gather<br/>sub-tasks in level]
    EL --> SC{_should_continue_or_pause}
    SC -->|awaiting_approval<br/>paused_at_level set| Pause([END · pause<br/>Redis: appr:graph_thread])
    SC -->|error| End2([END])
    SC -->|more levels| EL
    SC -->|all done| End3([END · completed])

    Pause -.->|PR-V.2 /resume| EL

    style MSD fill:#fff59d
    style EL fill:#ce93d8
    style SC fill:#ffcc80
    style Pause fill:#ef9a9a
    style End3 fill:#a5d6a7
```

---

## 5. Resume Parser Graph (7-step Linear)

```mermaid
stateDiagram-v2
    [*] --> validate
    validate --> parse: content/file_url ok
    validate --> [*]: missing input (error)
    parse --> confidence: result ok
    parse --> [*]: parse failed (error)
    confidence --> quality: score computed
    quality --> risk: 0-100 score
    risk --> dedup: red_flags extracted
    dedup --> output: duplicate check via Qdrant
    output --> [*]: parsed_data + verdict
```

---

## 6. Sequence Diagram: POST `/api/v1/agent/chat` (Single Intent)

```mermaid
sequenceDiagram
    autonumber
    actor U as User (Browser)
    participant FE as Next.js<br/>(dashboard)/agent
    participant API as FastAPI<br/>/api/v1/agent/chat
    participant Auth as Auth Middleware
    participant Cntl as agent.controller
    participant Orch as OrchestratorGraph<br/>(StateGraph)
    participant Router as RouterAgent
    participant Spec as Specialist Agent<br/>(e.g. screening)
    participant Svc as ScreeningService
    participant DB as PostgreSQL
    participant LLM as OMLX / vLLM

    U->>FE: Type "筛选张三的简历"
    FE->>API: POST /agent/chat {message, user_id}
    API->>Auth: verify JWT
    Auth-->>API: user_id validated
    API->>Cntl: chat(request)
    Cntl->>Orch: invoke(OrchestratorState)
    Orch->>Router: classify({"text": message})
    Router->>LLM: chat(system_prompt + user_msg)
    LLM-->>Router: {"intent": "screening"}
    Router-->>Orch: intent=screening
    Orch->>Spec: agent.run({input_text})
    Spec->>LLM: chat + tools
    LLM-->>Spec: tool_call: screening
    Spec->>Svc: screening.match(candidate_id)
    Svc->>DB: SELECT candidates JOIN jobs
    DB-->>Svc: match_score, evidence
    Svc-->>Spec: result dict
    Spec-->>Orch: {summary, result, status}
    Orch-->>Cntl: final state
    Cntl-->>API: {response, agent, status}
    API-->>FE: 200 JSON
    FE-->>U: Stream chunks via SSE
```

---

## 7. Sequence Diagram: Multi-Stage Orchestration (awaiting_approval pause)

```mermaid
sequenceDiagram
    autonumber
    actor U as User
    participant API as FastAPI<br/>/orchestrator/run
    participant Graph as OrchestratorGraph
    participant LLM as LLM
    participant HL as HumanLoopAgent
    participant Approval as ApprovalService
    participant Redis as Redis<br/>(appr:graph_thread)
    participant Spec as Specialist Agent

    U->>API: "帮我给候选人安排面试" + "生成 offer"
    API->>Graph: invoke(state)
    Graph->>LLM: decompose → JSON
    LLM-->>Graph: [interview, offering]
    Graph->>Graph: build_dag → [[0,1]]
    Graph->>Spec: asyncio.gather(interview, offering)
    Spec->>HL: create_proposal(thread_id=t1)
    HL->>Approval: create(approval, params)
    Approval->>Redis: SET appr:graph_thread:{aid} = t1
    Approval-->>HL: approval_id
    HL-->>Spec: status=awaiting_approval
    Spec-->>Graph: paused_at_level=0
    Graph-->>API: status=awaiting_approval

    U->>API: POST /human-loop/approvals/{id}/approve
    API->>Approval: approve(approval_id)
    Approval-->>API: status=approved
    API->>Graph: invoke(state, thread_id=t1)<br/>(resume)
    Graph->>Spec: continue execute_level
    Spec-->>Graph: completed
    Graph-->>API: status=completed
```

---

## 8. Sequence Diagram: Resume Upload + Parse (POST /resume)

```mermaid
sequenceDiagram
    autonumber
    actor U as HR User
    participant FE as /candidates page
    participant UP as POST /file/upload
    participant Minio as MinIO
    participant RPR as POST /resume/parse
    participant Graph as ResumeParserGraph<br/>(7-step)
    participant LLM as LLM
    participant Qdrant as Qdrant
    participant DB as PostgreSQL

    U->>FE: Drop resume.pdf
    FE->>UP: multipart upload
    UP->>Minio: put_object(bucket, key, bytes)
    Minio-->>UP: file_url
    UP-->>FE: {file_url, file_id}
    FE->>RPR: POST {file_url, target_job_id}
    RPR->>Graph: invoke(state)
    Graph->>Graph: validate ✓
    Graph->>Graph: parse → _handle_parse_resume()
    Graph->>LLM: extract structured fields
    LLM-->>Graph: parsed_data
    Graph->>Graph: confidence (LLM self-score)
    Graph->>Graph: quality (heuristic 0-100)
    Graph->>Graph: risk (red_flags detection)
    Graph->>Qdrant: search(similar candidates)
    Qdrant-->>Graph: top-K
    Graph->>Graph: dedup → is_duplicate?
    Graph-->>RPR: {parsed_data, quality, flags}
    RPR->>DB: INSERT candidate
    RPR-->>FE: {candidate_id, status}
```

---

## 9. Database ER Diagram

```mermaid
erDiagram
    User ||--o{ Application : "owns"
    User ||--o{ ConversationSession : "creates"
    User ||--o{ OperationLog : "audit"
    User ||--o{ Approval : "approver"
    User ||--o{ CommandAuditLog : "issues"

    Candidate ||--o{ Application : "applies"
    Candidate ||--o{ Interview : "scheduled"
    Candidate ||--o{ InterviewEvaluation : "evaluated"
    Candidate ||--o{ MemoryFact : "remembers"

    JobPosition ||--o{ Application : "receives"
    JobPosition ||--o{ Interview : "interview for"
    JobPosition ||--o{ Recommendation : "suggested"

    Application }o--|| Candidate : ""
    Application }o--|| JobPosition : ""

    Interview ||--o{ InterviewEvaluation : "has"
    Interview }o--|| Candidate : ""
    Interview }o--|| JobPosition : ""

    ConversationSession ||--o{ ConversationMessage : "contains"
    ConversationSession }o--|| User : ""

    MemoryFact }o--|| Candidate : ""
    MemoryFact }o--|| User : ""

    OperationLog }o--|| User : "actor"
    OperationStatsHourly ||--|| OperationLog : "rollup"

    MCPServer ||--o{ OperationLog : "tool_calls"

    Recommendation }o--|| JobPosition : "for"
    Recommendation }o--|| Candidate : "for"

    User {
        string id PK
        string email UK
        string role
        string hashed_password
    }
    Candidate {
        string id PK
        string name
        string email UK
        string status
        jsonb skills
    }
    JobPosition {
        string id PK
        string title
        string status
        text jd_text
    }
    Application {
        string id PK
        string candidate_id FK
        string job_id FK
        string stage
        float match_score
    }
    Interview {
        string id PK
        string candidate_id FK
        string job_id FK
        datetime scheduled_at
        string status
        string type
    }
    InterviewEvaluation {
        string id PK
        string interview_id FK
        float overall_score
        string verdict
    }
    ConversationSession {
        string id PK
        string user_id FK
        string intent
    }
    ConversationMessage {
        string id PK
        string session_id FK
        string role
        text content
    }
    MemoryFact {
        string id PK
        string user_id FK
        string candidate_id FK
        text content
        jsonb embedding
    }
    OperationLog {
        string id PK
        string user_id FK
        string agent_name
        string action
        string status
    }
    OperationStatsHourly {
        datetime hour_bucket PK
        string agent_name
        int total_ops
        float avg_duration_ms
    }
    MCPServer {
        string id PK
        string name UK
        string server_url
        bool enabled
    }
    Approval {
        string id PK
        string user_id FK
        string action_type
        string status
    }
    Recommendation {
        string id PK
        string candidate_id FK
        string job_id FK
        float score
    }
```

---

## 10. API Surface (27 Routers)

| Prefix | Tag | Purpose |
|---|---|---|
| `/api/v1/auth` | Auth | Login, register, refresh |
| `/api/v1/agent` | Agent | Single-agent chat |
| `/api/v1/pipeline` | Pipeline | Sequential screening |
| `/api/v1/router` | Router | Intent classification |
| `/api/v1/parallel` | Parallel | Aggregator pattern |
| `/api/v1/orchestrator` | Orchestrator | Multi-stage DAG |
| `/api/v1/loop` | Loop | Gen-Eval loop |
| `/api/v1/human-loop` | Human Loop | Approval workflow |
| `/api/v1/mcp` | MCP | Server registry |
| `/api/v1/tools` | Tools | Tool discovery + exec |
| `/api/v1/retrieval` | Retrieval | Vector search |
| `/api/v1/knowledge` | Knowledge | RAG KB |
| `/api/v1/memory` | Memory | Cross-session facts |
| `/api/v1/candidates` | Candidates | CRUD |
| `/api/v1/jobs` | Jobs | CRUD |
| `/api/v1/applications` | Applications | CRUD |
| `/api/v1/interviews` | Interviews | Schedule + manage |
| `/api/v1/evaluations` | Evaluations | Scoring |
| `/api/v1/screen` | Screening | Match + score |
| `/api/v1/conversation` | Conversation | Multi-turn |
| `/api/v1/recommendations` | Recommendations | Proactive |
| `/api/v1/operations` | Operations | Audit log |
| `/api/v1/audit` | Audit | Command audit |
| `/api/v1/tasks` | Tasks | Async task list |
| `/api/v1/dashboard` | Dashboard | Stats + reports |
| `/api/v1/resume` | Resume | Parse endpoint |
| `/api/v1/file` | File | MinIO upload |
| `/api/v1/settings` | Settings | Per-user config |
| `/api/v1/summaries` | Summaries | Cross-session |

---

## 11. Frontend Page Map (Next.js 14 App Router)

```mermaid
flowchart TB
    Root["/ (redirect)"] --> Login["(auth)/login"]
    Login --> Dash["(dashboard)/layout"]
    Dash --> Agent["/agent<br/>Chat + CommandPalette"]
    Dash --> Cand["/candidates"]
    Dash --> Jobs["/jobs"]
    Dash --> Inter["/interview"]
    Dash --> Screen["/screening"]
    Dash --> Eval["/evaluation"]
    Dash --> Reports["/reports"]
    Dash --> KB["/knowledge"]
    Dash --> Audit["/audit"]
    Dash --> Set["/settings"]
    Dash --> JD["/jd-generator"]
    Dash --> MCP["/mcp-servers"]
    Dash --> Tal["/talent-profile"]
    Dash --> Board["/dashboard"]
```

---

## 12. Component Architecture (Frontend)

```mermaid
flowchart LR
    Page["page.tsx<br/>(Server Component)"] --> Layout["layout.tsx<br/>(Client)"]
    Layout --> Query["React Query<br/>(data fetch)"]
    Query --> Comp["Feature Components"]
    Comp --> UI["UI Primitives<br/>(Button · Card · ...)"]
    Comp --> Hooks["Custom Hooks<br/>useChatStream · useCommandPalette"]
    Hooks --> Store["Zustand Stores<br/>auth · ui · conversation"]
    Comp --> SSE["SSE Client<br/>fetch + ReadableStream"]
    Comp --> Forms["React Hook Form + Zod"]

    style Page fill:#bbdefb
    style Comp fill:#ce93d8
    style UI fill:#a5d6a7
    style Store fill:#ffcc80
```

---

## 13. Background Loops (lifespan tasks)

```mermaid
flowchart LR
    Startup["FastAPI lifespan startup"] --> LoadMCP["Load enabled<br/>MCP servers<br/>from DB"]
    LoadMCP --> InitAgents["init_agents()<br/>register all specialists"]
    InitAgents --> Sched["asyncio.create_task<br/>recommendation_scheduler_loop"]
    InitAgents --> Agg["asyncio.create_task<br/>aggregation_loop (5min)"]

    Sched -.->|every N min| RecScan["Scan recent candidates<br/>+ active jobs<br/>→ push Recommendation"]
    Agg -.->|every 5 min| Stats["UPSERT<br/>operation_stats_hourly"]

    style Startup fill:#fff59d
    style Sched fill:#ce93d8
    style Agg fill:#a5d6a7
```

---

## 14. Key Design Patterns

| Pattern | Where | Why |
|---|---|---|
| **StateGraph** | orchestrator_graph · resume_parser_graph | Long-running, interruptable, checkpoint |
| **Registry** | `AgentRegistry.resolve(name)` | Decouple intent → agent mapping |
| **Tool Calling** | All agents via `llm.bind_tools()` | LLM-decided function invocation |
| **Soft Delete + Audit** | `operation_log` immutable | Every state change tracked |
| **Multi-stage DAG** | `build_dag()` topological sort | Parallel + dependent sub-tasks |
| **Redis Pub/Sub** | SSE for chat, approval events | Real-time UI updates |
| **Rate Limit** | Custom middleware (decorator-based) | Avoid Starlette #1334 |
| **LLM Retry** | `agent_service.py` exponential backoff | OMLX flaky connections |
| **Pydantic v2** | All schemas + FastAPI | Type-safe DTOs |
| **Checkpoint** | LangGraph `MemorySaver` | Resume after crash/approval |

---

*All diagrams render natively in GitHub, GitLab, VS Code, and any Mermaid-compatible viewer.*
